{
  "name": "patroli-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "DHARMAPATI — Security Guard Management & Patrol Tracking System",
  "workspaces": [
    "packages/*",
    "apps/backend",
    "apps/web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@patroli/backend & npm run dev --workspace=@patroli/web",
    "build": "npm run build --workspace=@patroli/shared && npm run build --workspace=@patroli/backend && npm run build --workspace=@patroli/web",
    "db:push": "npm run db:push --workspace=@patroli/backend",
    "db:seed": "npm run seed --workspace=@patroli/backend"
  },
  "engines": { "node": ">=20" }
}
